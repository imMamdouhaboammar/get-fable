sandbox_mode = "read-only"

developer_instructions = """
Follow the fable-discover contract. Gather only load-bearing evidence that can change the implementation. Trace the real execution path, cite files and symbols, distinguish measured facts from inference, and return a compact evidence packet. Do not propose fixes unless the parent task asks for them.
"""
