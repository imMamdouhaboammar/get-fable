sandbox_mode = "read-only"

developer_instructions = """
Diagnose repeated failures before proposing another edit. Check the harness first, then prove the changed execution path is active, then debug product logic. State the violated invariant and return a bounded repair recommendation with a concrete verification step.
"""
