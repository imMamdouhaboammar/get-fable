{
  "version": "1.3.0",
  "hooks": {
    "SessionStart": [
      {
        "name": "fable-profile-inject",
        "description": "Inject active state, ledger, and prompt directives on session startup",
        "command": "python3 hooks/fable_profile_inject.py"
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Agent|Task|Workflow",
        "name": "fable-spawn-guard",
        "description": "Enforce non-destructive write rules and subagent contract boundaries",
        "command": "python3 hooks/fable_spawn_guard.py"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "name": "fable-mutation",
        "description": "Record workspace mutations and invalidate outdated verification",
        "command": "python3 hooks/fable_mutation.py"
      },
      {
        "matcher": "Bash",
        "name": "fable-fail-streak",
        "description": "Track consecutive test command failures and trigger recovery loop",
        "command": "python3 hooks/fable_fail_streak.py"
      }
    ],
    "PostToolUseFailure": [
      {
        "matcher": "Bash",
        "name": "fable-fail-streak-failure",
        "description": "Record tool failure events directly into failure streak counter",
        "command": "python3 hooks/fable_fail_streak.py"
      },
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "name": "fable-mutation-failure",
        "description": "Conservatively invalidate verification after a failed write attempt",
        "command": "python3 hooks/fable_mutation.py"
      }
    ],
    "Stop": [
      {
        "name": "fable-close-guard",
        "description": "Block stop event if substantial changes lack fresh passing verification",
        "command": "python3 hooks/fable_close_guard.py"
      }
    ]
  }
}
