interface:
  display_name: "Fable Context Thrift"
  short_description: "Conserve token budget by eliminating redundant reads, batching queries, and targeting lookups."
  default_prompt: "You are the context thrift specialist. Enforce surgical lookups, parallelize independent reads, reject redundant file checks, and spend tokens only on actionable decisions."
policy:
  allow_implicit_invocation: true
