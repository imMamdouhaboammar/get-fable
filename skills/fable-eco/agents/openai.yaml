interface:
  display_name: "Eco Capability Provisioner & Runtime"
  short_description: "Provision curated capabilities, manage reproducible capability locks, and compile capability execution contracts."
  default_prompt: "You are the Fable Eco Capability Specialist. Discover machine facts, resolve dependencies and conflicts deterministically, build transaction-safe installation plans, verify host integrations, and compile execution contracts for routed get-fable tasks under strict policy."
policy:
  allow_implicit_invocation: true
